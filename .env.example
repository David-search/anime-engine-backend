# Copy to .env (gitignored). Compose reads it for ${VAR} substitution.
CONTAINER_NAME=anime-backend
BACKEND_HOST_PORT=8000          # local 8000 ; server 8008 (vast public 43577)
CORS_ORIGINS=*
TEST_STREAM=https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8

# Integration (when wiring social/search). See claude/docs/infrastructure.md
# MONGO_URI=mongodb://admin:PASSWORD@mongodb:27017/anime_db?authSource=admin
# ELASTIC_URL=http://elasticsearch:9200
# ELASTIC_USER=elastic
# ELASTIC_PASSWORD=PASSWORD
# ELASTIC_INDEX=anime
MONGO_URI=
ELASTIC_URL=
ELASTIC_USER=
ELASTIC_PASSWORD=
ELASTIC_INDEX=anime
